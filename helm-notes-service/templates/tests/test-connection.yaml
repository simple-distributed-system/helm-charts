apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notes-service.fullname" . }}-test-connection"
  labels:
    {{- include "notes-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notes-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
