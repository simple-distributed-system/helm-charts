apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-service-a.fullname" . }}-test-connection"
  labels:
    {{- include "helm-service-a.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-service-a.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
